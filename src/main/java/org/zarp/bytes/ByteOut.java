package org.zarp.bytes;

/**
 * Represents an accessor for writing data into a stream or buffer, combines random access and sequential access.
 */
public interface ByteOut extends RandomOutput, StreamOutput {
}
